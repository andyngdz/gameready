/target
**/*.rs.bk
*.pdb

# insta writes these when a snapshot changes; review with `cargo insta review`
**/snapshots/*.snap.new

# agent memories are local-only, not shared through the repo
/memories/
